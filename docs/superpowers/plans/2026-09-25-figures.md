# Схемы в теории, первая порция (SQL) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Механизм SVG-схем в теории уроков и первые четыре схемы модуля 1 (порядок выполнения запроса, JOIN, окна, когорты).

**Architecture:** Схемы генерирует `инструменты/figs.py` из `инструменты/shop.db` и записывает блоком `/* FIGS:BEGIN … FIGS:END */` в `content-m1.js` как `window.FIGS[id] = "<svg…>"`. В теории урока стоит `<figure class="fig" data-fig="id">`, `Figs.mount` в `app.js` вставляет туда SVG после разметки терминов. Цвета — только классы `.f-*` на токенах темы. `инструменты/checkfigs.js` проверяет связность, доступность, ширину и отсутствие жёстких цветов.

**Tech Stack:** Python 3 (sqlite3, стандартная библиотека), Node (проверка), ванильный JS/CSS сайта.

**Spec:** `docs/superpowers/specs/2026-09-25-figures-design.md`

## Global Constraints

- Стиль «Тетрадь»: без заливок, кроме `.f-tint` у когорт; ручка `var(--pen)` только на главном; реплика — `var(--hand)`.
- `viewBox` шириной не больше 340; шрифт данных 12.5, подписей 10.5–12.5, реплики 15.
- На компьютере `figure.fig` по центру, не шире 520 px.
- В SVG нет цветов напрямую (`#…`, `rgb(`, `fill="black"` и т. п.) — только классы.
- У каждого SVG `role="img"`, `<title>` и `<desc>` с выводом словами; текст — `<text>`, не кривые; без анимации.
- Числа на схемах — как их выводит SQLite в курсе (`2822.77`, `15301.0`, `15.2`).
- `figs.py` детерминирован: повторный запуск без изменений данных даёт тот же файл.
- Перед выкладкой поднять `VERSION` в `sw.js` и пересобрать `Тетрадь аналитика.html` (`python3 build.py`).
- Работа в ветке `stage3-figures`; коммиты с `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- Исполнение: в этой сессии, по задачам, после каждой задачи — коммит, отчёт пользователю и ожидание «продолжай».

## Review Focus

- Разметка терминов (`Terms.mark`) заходит внутрь SVG и ломает `<text>` — ожидается: схема вставляется после разметки, внутри `svg` нет `.term`. Тест — Task 1, шаг 7.
- Повторная отрисовка урока (возврат «назад», смена темы) удваивает схему — ожидается: `Figs.mount` пропускает `figure`, где `svg` уже есть. Тест — Task 1, шаг 7.
- Тёмная тема: линии или текст пропадают на тёмной бумаге — ожидается: всё видно, цвета только из токенов. Проверка — `checkfigs.js` (жёсткие цвета) и осмотр в Task 5.
- Телефон 390 px: схема вызывает горизонтальную прокрутку страницы — ожидается: `scrollWidth` равен ширине окна. Проверка — каждая задача и Task 5.
- Метка `data-fig` с опечаткой — ожидается: ученик не видит пустую рамку (`figure` убирается), а `checkfigs.js` падает до выкладки. Тест — Task 1, шаги 3 и 7.

---

## Файлы

- Create: `инструменты/figs.py` — генератор схем модуля 1 (помощники рисования, выборки из базы, запись блока в `content-m1.js`, `--check` со сверкой чисел).
- Create: `инструменты/checkfigs.js` — проверка схем по всем модулям.
- Modify: `app.js` — объект `Figs` (после `Plots`), вызов `Figs.mount` в `renderLesson` после `Terms.mark`.
- Modify: `styles.css` — раздел «Схемы» в конце файла.
- Modify: `content-m1.js` — метки `<figure>` в теории m1l1, m1l2, m1l6 и сгенерированный блок `FIGS`.
- Modify: `инструменты/README.md` — строки про `figs.py` и `checkfigs.js`.
- Modify (Task 5): `sw.js` (`VERSION`), `Тетрадь аналитика.html` (сборка).

---

### Task 1: Механизм схем и схема «Порядок выполнения запроса»

**Files:**
- Create: `инструменты/checkfigs.js`, `инструменты/figs.py`
- Modify: `app.js`, `styles.css`, `content-m1.js` (m1l1), `инструменты/README.md`

**Interfaces:**
- Produces: `window.FIGS: {[id]: string}`; `Figs.mount(root: Element): void`; в `figs.py` — класс `Svg(fid, h, title, desc)` с методами `text(x, y, s, cls="f-t", size=12.5, anchor=None)`, `line(x1, y1, x2, y2, cls="f-row")`, `rect(x, y, w, h, cls="f-box", rx=5, extra="")`, `path(d, cls="f-pen")`, `circle(cx, cy, r, cls="f-pen-fill")`, `note(x, y, lines, size=15)`, `render() -> str`; функции `num(v) -> str`, `db() -> sqlite3.Connection`, `table(svg, x, y, w, title, head, rows, row_h=26) -> list[float]` (центры строк по y), `write_block(module: str, figs: dict[str, str])`; реестр `FIGS_M1: dict[str, callable]`; `check_m1() -> list[str]`.

- [ ] **Step 1: Метка схемы в теории m1l1**

В `content-m1.js`, теория `m1l1`, сразу после первого абзаца под `<h3>Порядок выполнения запроса</h3>`:

```html
<figure class="fig" data-fig="sql-order"><figcaption>Рис. Запрос пишут в одном порядке, а база выполняет в другом</figcaption></figure>
```

- [ ] **Step 2: Написать `инструменты/checkfigs.js`**

```js
#!/usr/bin/env node
/* Проверка схем в теории (window.FIGS, см. инструменты/figs.py):
   - каждая метка data-fig в теории указывает на схему, каждая схема
     где-то стоит;
   - у схемы role="img", <title> и <desc> — её читает диктор;
   - viewBox не шире 340 — схемы рисуются под телефон;
   - цвета только классами .f-*: заданный напрямую цвет пропадёт
     в тёмной теме.
   Запуск: node инструменты/checkfigs.js                          */
const fs = require("fs"), vm = require("vm"), path = require("path");
const base = path.resolve(__dirname, "..");
const ctx = { window: { SH: {}, CONTENT: {}, DATA: {} }, console };
vm.createContext(ctx);
for (const f of ["content-core.js"].concat(
       fs.readdirSync(base).filter(f => /^content-m\d+\.js$/.test(f)).sort())) {
  vm.runInContext(fs.readFileSync(path.join(base, f), "utf8"), ctx);
}
const FIGS = ctx.window.FIGS || {};
const bad = [], used = {};
for (const [id, C] of Object.entries(ctx.window.CONTENT)) {
  for (const m of String(C.theory || "").matchAll(/data-fig="([\w-]+)"/g)) {
    used[m[1]] = true;
    if (!FIGS[m[1]]) bad.push(id + ": метка data-fig=\"" + m[1] + "\", а схемы нет — запустите figs.py");
  }
}
for (const [fid, svg] of Object.entries(FIGS)) {
  if (!used[fid]) bad.push("схема " + fid + " нигде не стоит в теории");
  if (!/role="img"/.test(svg)) bad.push(fid + ": нет role=\"img\"");
  if (!/<title[ >]/.test(svg) || !/<desc[ >]/.test(svg)) bad.push(fid + ": нет <title> или <desc>");
  const vb = svg.match(/viewBox="0 0 ([\d.]+) ([\d.]+)"/);
  if (!vb) bad.push(fid + ": нет viewBox");
  else if (+vb[1] > 340) bad.push(fid + ": viewBox шириной " + vb[1] + " — больше 340");
  if (/#[0-9a-f]{3,8}\b|rgba?\(|(fill|stroke)="(?!none)[a-z]/i.test(svg.replace(/url\(#[\w-]+\)/g, "")))
    bad.push(fid + ": цвет задан напрямую — только классы .f-*");
}
console.log("Схем: " + Object.keys(FIGS).length + ", меток в теории: " + Object.keys(used).length);
if (bad.length) { console.log("\nОшибки:\n" + bad.join("\n")); process.exit(1); }
console.log("Ошибок нет.");
```

- [ ] **Step 3: Запустить — должна упасть**

Run: `node инструменты/checkfigs.js`
Expected: exit 1, строка `m1l1: метка data-fig="sql-order", а схемы нет — запустите figs.py`

- [ ] **Step 4: Написать `инструменты/figs.py` с помощниками и схемой `sql-order`**

```python
#!/usr/bin/env python3
"""Схемы в теории уроков (стиль «Тетрадь»).

Строит SVG из настоящих строк базы курса и записывает их блоком
/* FIGS:BEGIN … FIGS:END */ в content-mN.js как window.FIGS[id].
Цветов в SVG нет — только классы .f-* (styles.css, раздел «Схемы»),
поэтому тёмная тема работает сама. viewBox шириной W: схема рисуется
под телефон и на компьютере не растягивается шире 520 px.

    python3 инструменты/figs.py m1           # пересобрать схемы модуля 1
    python3 инструменты/figs.py m1 --check   # только сверить числа с базой
"""
import html
import json
import pathlib
import re
import sqlite3
import sys

ROOT = pathlib.Path(__file__).resolve().parent.parent
DB = ROOT / "инструменты" / "shop.db"
W = 340


def esc(s):
    return html.escape(str(s), quote=True)


def num(v):
    """Число так, как его показывает SQLite в курсе: 2822.77, 15301.0."""
    return repr(round(float(v), 2))


def db():
    return sqlite3.connect(DB)


class Svg:
    def __init__(self, fid, h, title, desc):
        self.fid, self.h, self.title, self.desc = fid, h, title, desc
        self.parts = []

    def text(self, x, y, s, cls="f-t", size=12.5, anchor=None):
        a = f' text-anchor="{anchor}"' if anchor else ""
        self.parts.append(f'<text class="{cls}" x="{x:g}" y="{y:g}" font-size="{size:g}"{a}>{esc(s)}</text>')

    def line(self, x1, y1, x2, y2, cls="f-row"):
        self.parts.append(f'<line class="{cls}" x1="{x1:g}" y1="{y1:g}" x2="{x2:g}" y2="{y2:g}"/>')

    def rect(self, x, y, w, h, cls="f-box", rx=5, extra=""):
        self.parts.append(f'<rect class="{cls}" x="{x:g}" y="{y:g}" width="{w:g}" height="{h:g}" rx="{rx:g}"{extra}/>')

    def path(self, d, cls="f-pen"):
        self.parts.append(f'<path class="{cls}" d="{d}"/>')

    def circle(self, cx, cy, r, cls="f-pen-fill"):
        self.parts.append(f'<circle class="{cls}" cx="{cx:g}" cy="{cy:g}" r="{r:g}"/>')

    def note(self, x, y, lines, size=15):
        for i, s in enumerate(lines):
            self.text(x, y + i * (size + 3), s, "f-note", size)

    def render(self):
        t, d = f"fig-{self.fid}-t", f"fig-{self.fid}-d"
        return (f'<svg class="fig-svg" viewBox="0 0 {W} {self.h:g}" role="img" aria-labelledby="{t} {d}">'
                f'<title id="{t}">{esc(self.title)}</title><desc id="{d}">{esc(self.desc)}</desc>'
                + "".join(self.parts) + "</svg>")


def table(svg, x, y, w, title, head, rows, row_h=26):
    """Таблица-рамка: заголовок над ней, подпись столбцов, строки.
    rows — строки текста (моноширинный, колонки выровнены пробелами
    заранее). Возвращает центры строк по y."""
    svg.text(x + 2, y - 7, title, "f-hd", 12.5)
    h = 22 + row_h * len(rows)
    svg.rect(x, y, w, h)
    svg.text(x + 8, y + 15, head, "f-sub", 10.5)
    mids = []
    for i, r in enumerate(rows):
        top = y + 22 + i * row_h
        svg.line(x, top, x + w, top)
        svg.text(x + 8, top + row_h / 2 + 4.5, r)
        mids.append(top + row_h / 2)
    return mids


# ---------------------------------------------------------------- схемы m1

def fig_sql_order():
    written = ["SELECT", "FROM / JOIN", "WHERE", "GROUP BY", "HAVING", "ORDER BY", "LIMIT"]
    run = ["FROM / JOIN", "WHERE", "GROUP BY", "HAVING", "SELECT", "ORDER BY", "LIMIT"]
    svg = Svg("sql-order", 262,
              "Порядок записи и порядок выполнения SQL-запроса",
              "Пишем: SELECT, FROM, WHERE, GROUP BY, HAVING, ORDER BY, LIMIT. "
              "Выполняется: FROM и JOIN, WHERE, GROUP BY, HAVING, SELECT вместе с оконными функциями, ORDER BY, LIMIT. "
              "Поэтому псевдоним из SELECT нельзя использовать в WHERE: на этом шаге его ещё нет.")
    svg.text(0, 14, "пишем", "f-hd", 12.5)
    svg.text(196, 14, "выполняется", "f-hd", 12.5)
    y0, step = 34, 26
    ly = {}
    for i, w in enumerate(written):
        y = y0 + i * step
        svg.rect(0, y - 15, 118, 22, rx=4)
        svg.text(8, y, w)
        ly[w] = y - 4
    ry = {}
    for i, w in enumerate(run):
        y = y0 + i * step
        svg.rect(196, y - 15, 144, 22, rx=4)
        svg.text(204, y, f"{i + 1} {w}")
        ry[w] = y - 4
    svg.text(338, ry["SELECT"] + 4, "+ окна", "f-sub", 10.5, anchor="end")
    for w in written:
        cls = "f-pen" if w == "SELECT" else "f-soft"
        svg.path(f"M118 {ly[w]:g} C 157 {ly[w]:g}, 157 {ry[w]:g}, 196 {ry[w]:g}", cls)
    svg.note(0, 222, ["псевдоним из SELECT ещё", "не существует в WHERE"])
    return svg.render()


FIGS_M1 = {
    "sql-order": fig_sql_order,
}


# ---------------------------------------------------------------- проверка чисел

def check_m1():
    """Числа, которые стоят на схемах, — ровно те, что даёт база."""
    return []


# ---------------------------------------------------------------- запись

def write_block(module, figs):
    p = ROOT / f"content-{module}.js"
    s = p.read_text(encoding="utf8")
    body = "window.FIGS = window.FIGS || {};\n" + "".join(
        f"window.FIGS[{json.dumps(k)}] = {json.dumps(v, ensure_ascii=False)};\n" for k, v in figs.items())
    block = ("/* FIGS:BEGIN — схемы генерирует инструменты/figs.py, руками не править */\n"
             + body + "/* FIGS:END */")
    if "/* FIGS:BEGIN" in s:
        s = re.sub(r"/\* FIGS:BEGIN.*?/\* FIGS:END \*/", lambda m: block, s, flags=re.S)
    else:
        s = s.rstrip("\n") + "\n\n" + block + "\n"
    p.write_text(s, encoding="utf8")


MODULES = {"m1": (FIGS_M1, check_m1)}

if __name__ == "__main__":
    mod = sys.argv[1] if len(sys.argv) > 1 else ""
    if mod not in MODULES:
        sys.exit("укажите модуль: " + ", ".join(MODULES))
    figs, check = MODULES[mod]
    errs = check()
    if errs:
        sys.exit("Числа не сошлись с базой:\n" + "\n".join(errs))
    if "--check" in sys.argv:
        print("Числа сходятся с базой.")
        sys.exit(0)
    write_block(mod, {k: f() for k, f in figs.items()})
    print(f"content-{mod}.js: схем {len(figs)} — " + ", ".join(figs))
```

- [ ] **Step 5: Сгенерировать и проверить**

Run: `python3 инструменты/figs.py m1 && node инструменты/checkfigs.js && md5 -q content-m1.js && python3 инструменты/figs.py m1 && md5 -q content-m1.js`
Expected: `content-m1.js: схем 1 — sql-order`, `Ошибок нет.`, две одинаковые контрольные суммы (детерминированность).

- [ ] **Step 6: `Figs` в `app.js` и стили**

В `app.js` сразу после объекта `Plots` (перед блоком «Проверка результата»):

```js
/* ============================================================
   Схемы в теории

   SVG схем лежат в window.FIGS (блок FIGS в content-mN.js, его пишет
   инструменты/figs.py). В теории на месте схемы — <figure class="fig"
   data-fig="id"> с подписью; mount вставляет SVG перед подписью.
   Вызывается после разметки терминов: иначе Terms.mark залез бы
   в <text> схемы. Повторный вызов ничего не удваивает. Схемы нет —
   рамка убирается целиком, чтобы ученик не видел пустое место.
   ============================================================ */

const Figs = {
  mount: function (root) {
    if (!root) return;
    Array.prototype.forEach.call(root.querySelectorAll("figure[data-fig]"), function (f) {
      if (f.querySelector("svg")) return;
      const id = f.getAttribute("data-fig"), svg = (window.FIGS || {})[id];
      if (!svg) { console.warn("Нет схемы " + id); f.remove(); return; }
      f.insertAdjacentHTML("afterbegin", svg);
    });
  }
};
```

В `renderLesson` сразу после `Terms.mark($("#s-theory .theory"));`:

```js
  Figs.mount($("#s-theory .theory"));
```

В конец `styles.css`:

```css
/* ============================================================
   Схемы в теории (Figs в app.js, SVG — инструменты/figs.py)
   Стиль «Тетрадь»: тонкая линия без заливки, ручка — только на
   главном. Цвета — только токены: тёмная тема работает сама.
   ============================================================ */
.theory > figure.fig { max-width: 520px; margin: 26px auto 30px; }
figure.fig .fig-svg { display: block; width: 100%; height: auto; overflow: visible; }
figure.fig figcaption {
  margin-top: 10px; text-align: center;
  font: 13px/1.45 var(--sans); color: var(--ink-3);
}
.fig-svg text   { font-family: var(--mono); fill: var(--ink); }
.fig-svg .f-hd  { font-family: var(--sans); font-weight: 600; fill: var(--ink-2); }
.fig-svg .f-sub { font-family: var(--sans); fill: var(--ink-3); }
.fig-svg .f-note { font-family: var(--hand); fill: var(--pen); }
.fig-svg .f-box { fill: none; stroke: var(--rule-hard); stroke-width: 1; }
.fig-svg .f-row { stroke: var(--rule); stroke-width: .8; }
.fig-svg .f-soft { fill: none; stroke: var(--rule-hard); stroke-width: 1; }
.fig-svg .f-pen { fill: none; stroke: var(--pen); stroke-width: 1.4; }
.fig-svg .f-pen-fill { fill: var(--pen); }
.fig-svg .f-tint { fill: var(--pen); stroke: none; }
```

- [ ] **Step 7: Проверить в браузере (Review Focus: термины, повтор, опечатка)**

Запустить `python3 инструменты/devserver.py 8779`, в тестовом браузере снять регистрацию service worker и очистить `caches`, открыть `http://127.0.0.1:8779/#m1l1`. Проверить:
- в `figure[data-fig="sql-order"]` ровно один `svg`, внутри `svg` нет элементов разметки терминов (`.term`, `[data-term]`);
- перерисовка урока (`location.hash = "#m1l3"`, затем `#m1l1`) — по-прежнему один `svg`;
- временно поменять метку на `data-fig="nope"` в `content-m1.js`, перезагрузить: рамки схемы на странице нет, в консоли `Нет схемы nope`; `checkfigs.js` падает; вернуть метку.
- 390 и 1280 px, светлая и тёмная тема: линии и подписи видны, `scrollWidth === innerWidth`, на 1280 px ширина схемы ≤ 520.

- [ ] **Step 8: README инструментов и коммит**

В `инструменты/README.md` добавить:

```markdown
- `figs.py m1 [--check]` — генерирует SVG-схемы теории модуля в блок `FIGS` файла `content-m1.js`; `--check` только сверяет числа схем с `shop.db`.
- `checkfigs.js` — проверка схем: метки в теории ↔ схемы, `title`/`desc`, ширина `viewBox` ≤ 340, никаких цветов напрямую.
```

```bash
git add инструменты/figs.py инструменты/checkfigs.js инструменты/README.md app.js styles.css content-m1.js
git commit -m "Схемы в теории: механизм и «Порядок выполнения запроса» (1.2)

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Схема «JOIN размножает строки»

**Files:**
- Modify: `инструменты/figs.py`, `content-m1.js` (метка в m1l1 + блок)

**Interfaces:**
- Consumes: `Svg`, `table`, `num`, `db`, `write_block`, `FIGS_M1`, `check_m1` из Task 1.
- Produces: `join_data() -> (users: list[tuple[int, str]], orders: list[tuple[int, float]])`; схема `join-rows`.

- [ ] **Step 1: Сверка чисел (падает)**

В `figs.py` заменить тело `check_m1`:

```python
def check_m1():
    """Числа, которые стоят на схемах, — ровно те, что даёт база."""
    errs = []
    users, orders = join_data()
    if users != [(16, "Новосибирск"), (21, "Екатеринбург")]:
        errs.append(f"join-rows: пользователи {users}")
    if [(u, num(r)) for u, r in orders] != [(16, "2822.77"), (16, "1931.78"), (21, "5886.6")]:
        errs.append(f"join-rows: заказы {orders}")
    return errs
```

Run: `python3 инструменты/figs.py m1 --check`
Expected: FAIL — `NameError: name 'join_data' is not defined`

- [ ] **Step 2: Данные и схема**

```python
def join_data():
    """Пользователь с наименьшим id ровно с двумя заказами (любого статуса:
    JOIN в уроке без фильтра) и следующий за ним — ровно с одним."""
    c = db()
    two = c.execute("SELECT user_id FROM orders GROUP BY user_id HAVING COUNT(*) = 2 "
                    "ORDER BY user_id LIMIT 1").fetchone()[0]
    one = c.execute("SELECT user_id FROM orders GROUP BY user_id HAVING COUNT(*) = 1 AND user_id > ? "
                    "ORDER BY user_id LIMIT 1", (two,)).fetchone()[0]
    users = c.execute("SELECT user_id, city FROM users WHERE user_id IN (?, ?) ORDER BY user_id",
                      (two, one)).fetchall()
    orders = c.execute("SELECT user_id, revenue FROM orders WHERE user_id IN (?, ?) "
                       "ORDER BY user_id, order_date", (two, one)).fetchall()
    return users, orders


def fig_join_rows():
    users, orders = join_data()
    city = dict(users)
    dup = users[0][0]
    svg = Svg("join-rows", 318,
              "JOIN соединяет строки двух таблиц",
              f"У пользователя {dup} два заказа, у пользователя {users[1][0]} один. "
              f"После JOIN пользователь {dup} встречается в результате дважды: строк стало три, хотя пользователей два.")
    um = table(svg, 0, 22, 128, "users", "user_id city",
               [f"{u:<3}{c}" for u, c in users])
    om = table(svg, 234, 22, 106, "orders", "user_id revenue",
               [f"{u:<3}{num(r)}" for u, r in orders])
    ui = {u: um[i] for i, (u, _) in enumerate(users)}
    for i, (u, _) in enumerate(orders):
        a, b = ui[u], om[i]
        svg.path(f"M128 {a:g} C 181 {a:g}, 181 {b:g}, 234 {b:g}")
    svg.circle(128, ui[dup], 3.4)
    table(svg, 0, 176, 214, "users JOIN orders", "user_id city revenue",
          [f"{u:<3}{city[u]:<13}{num(r)}" for u, r in orders])
    svg.note(0, 300, [f"у {dup}-го два заказа — две строки"])
    return svg.render()
```

В `FIGS_M1` добавить `"join-rows": fig_join_rows,` после `"sql-order"`.

- [ ] **Step 3: Проверка чисел проходит**

Run: `python3 инструменты/figs.py m1 --check`
Expected: `Числа сходятся с базой.`

- [ ] **Step 4: Метка в теории**

В `content-m1.js`, теория `m1l1`, после первого абзаца раздела `<h3>Ловушка первая: строки размножаются</h3>` («У одного пользователя может быть пять заказов…»):

```html
<figure class="fig" data-fig="join-rows"><figcaption>Рис. JOIN соединяет строки — и размножает их</figcaption></figure>
```

- [ ] **Step 5: Сгенерировать, проверить, посмотреть**

Run: `python3 инструменты/figs.py m1 && node инструменты/checkfigs.js`
Expected: `схем 2`, `Ошибок нет.`
В браузере `#m1l1` (сбросить service worker): 390 и 1280 px, обе темы — три связи ручкой, точка у пользователя 16, подписи не налезают на рамки, нет горизонтальной прокрутки.

- [ ] **Step 6: Коммит**

```bash
git add инструменты/figs.py content-m1.js
git commit -m "Схема «JOIN размножает строки» в уроке 1.2

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Схема «Окна и рамка»

**Files:**
- Modify: `инструменты/figs.py`, `content-m1.js` (метка в m1l2 + блок)

**Interfaces:**
- Consumes: `Svg`, `num`, `db`, `FIGS_M1`, `check_m1`.
- Produces: `window_data() -> list[tuple[int, str, float, float]]` — `(user_id, order_date, revenue, running)`; схема `window-frame`.

- [ ] **Step 1: Сверка чисел (падает)**

В `check_m1` перед `return errs`:

```python
    w = window_data()
    if [(u, d, num(r), num(s)) for u, d, r, s in w] != [
            (1, "2024-06-20", "2997.2", "2997.2"), (1, "2024-07-22", "4968.24", "7965.44"),
            (1, "2024-08-18", "1994.76", "9960.2"), (3, "2024-04-23", "1361.84", "1361.84"),
            (3, "2024-05-26", "9071.54", "10433.38"), (3, "2024-07-08", "3071.02", "13504.4"),
            (3, "2024-07-31", "1796.6", "15301.0")]:
        errs.append(f"window-frame: {w}")
```

Run: `python3 инструменты/figs.py m1 --check`
Expected: FAIL — `NameError: name 'window_data' is not defined`

- [ ] **Step 2: Данные и схема**

```python
def window_data():
    """Два первых пользователя с 3–4 оплаченными заказами; накопительная
    сумма — тем же окном, что в уроке."""
    c = db()
    ids = [r[0] for r in c.execute(
        "SELECT user_id FROM orders WHERE status = 'paid' GROUP BY user_id "
        "HAVING COUNT(*) BETWEEN 3 AND 4 ORDER BY user_id LIMIT 2")]
    return c.execute(
        "SELECT user_id, order_date, revenue, "
        "SUM(revenue) OVER (PARTITION BY user_id ORDER BY order_date) "
        "FROM orders WHERE status = 'paid' AND user_id IN (?, ?) "
        "ORDER BY user_id, order_date", ids).fetchall()


def fig_window_frame():
    rows = window_data()
    first = rows[0][0]
    n1 = sum(1 for r in rows if r[0] == first)
    svg = Svg("window-frame", 300,
              "Окна PARTITION BY user_id и накопительная сумма",
              "Оплаченные заказы двух пользователей по дате. SUM(revenue) OVER (PARTITION BY user_id ORDER BY order_date) "
              "копит сумму внутри окна пользователя и начинается заново в следующем окне: "
              f"{num(rows[n1 - 1][3])} у первого, {num(rows[-1][3])} у второго.")
    x, y, rh, w = 0, 22, 26, 290
    svg.text(2, y - 7, "orders, оплаченные", "f-hd", 12.5)
    h = 22 + rh * len(rows)
    svg.rect(x, y, w, h)
    svg.text(8, y + 15, "user_id date       revenue накопительно", "f-sub", 10.5)
    for i, (u, d, r, s) in enumerate(rows):
        top = y + 22 + i * rh
        svg.line(x, top, x + w, top, "f-pen" if i == n1 else "f-row")
        svg.text(8, top + rh / 2 + 4.5, f"{u:<3}{d}")
        svg.text(190, top + rh / 2 + 4.5, num(r), anchor="end")
        svg.text(282, top + rh / 2 + 4.5, num(s), anchor="end")
    # рамка третьей строки второго окна: от начала окна до текущей строки
    a, b = y + 22 + n1 * rh + 3, y + 22 + (n1 + 3) * rh - 3
    svg.path(f"M{w + 5} {a:g} h7 V{b:g} h-7")
    svg.text(w + 15, (a + b) / 2 + 4, "рамка", "f-sub", 10.5)
    svg.note(0, y + h + 26, ["сумма начинается заново", "в каждом окне"])
    return svg.render()
```

В `FIGS_M1` добавить `"window-frame": fig_window_frame,`. Подпись «рамка» (10.5 px, ~30 px шириной) начинается с x = 305 и укладывается в 340.

- [ ] **Step 3: Проверка чисел проходит**

Run: `python3 инструменты/figs.py m1 --check`
Expected: `Числа сходятся с базой.`

- [ ] **Step 4: Метка в теории m1l2**

В `content-m1.js`, теория `m1l2`, раздел `<h3>Рамка окна: то, что почти всегда работает по умолчанию</h3>` — после блока кода с тремя примерами `SUM/AVG … OVER`:

```html
<figure class="fig" data-fig="window-frame"><figcaption>Рис. PARTITION BY режет строки на окна, ORDER BY внутри окна даёт накопительную сумму</figcaption></figure>
```

- [ ] **Step 5: Сгенерировать, проверить, посмотреть**

Run: `python3 инструменты/figs.py m1 && node инструменты/checkfigs.js`
Expected: `схем 3`, `Ошибок нет.`
В браузере `#m1l2`: 390 и 1280 px, обе темы — числа выровнены по правому краю, линия ручкой между окнами, скобка «рамка» у строк 4–6, справа ничего не обрезано.

- [ ] **Step 6: Коммит**

```bash
git add инструменты/figs.py content-m1.js
git commit -m "Схема «Окна и рамка» в уроке 1.3

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Схема «Когортный треугольник»

**Files:**
- Modify: `инструменты/figs.py`, `content-m1.js` (метка в m1l6 + блок)

**Interfaces:**
- Consumes: `Svg`, `db`, `FIGS_M1`, `check_m1`.
- Produces: `cohort_data() -> list[tuple[str, int, list[float | None]]]` — `(cohort_month, size, [cr_m0 … cr_m5])`, `None` — месяц ещё не прожит целиком; схема `cohort-triangle`.

- [ ] **Step 1: Сверка чисел (падает)**

В `check_m1` перед `return errs`:

```python
    want = [
        ("2024-01", 33, [15.2, 45.5, 45.5, 45.5, 45.5, 45.5]),
        ("2024-02", 34, [11.8, 35.3, 38.2, 38.2, 38.2, 38.2]),
        ("2024-03", 49, [20.4, 55.1, 61.2, 63.3, 63.3, 63.3]),
        ("2024-04", 34, [11.8, 41.2, 47.1, 47.1, 47.1, None]),
        ("2024-05", 44, [9.1, 38.6, 45.5, 45.5, None, None]),
        ("2024-06", 26, [11.5, 30.8, 34.6, None, None, None]),
    ]
    got = cohort_data()
    if got != want:
        errs.append(f"cohort-triangle: {got}")
```

Run: `python3 инструменты/figs.py m1 --check`
Expected: FAIL — `NameError: name 'cohort_data' is not defined`

- [ ] **Step 2: Данные и схема**

```python
LAST_FULL = "2024-09-01"   # заказы в базе по 11.09.2024: август — последний полный месяц


def cohort_data():
    """Доля когорты, купившей хотя бы раз к концу N-го месяца жизни
    (N = 0 — месяц регистрации). Месяц, не прожитый целиком, — None."""
    c = db()
    out = []
    for cm, cs, n in c.execute(
            "SELECT strftime('%Y-%m', signup_date), date(signup_date, 'start of month'), COUNT(*) "
            "FROM users GROUP BY 1 ORDER BY 1").fetchall():
        vals = []
        for k in range(6):
            end = c.execute("SELECT date(?, ?)", (cs, f"+{k + 1} month")).fetchone()[0]
            if end > LAST_FULL:
                vals.append(None)
                continue
            got = c.execute(
                "SELECT COUNT(*) FROM users u WHERE strftime('%Y-%m', u.signup_date) = ? AND EXISTS ("
                "SELECT 1 FROM orders o WHERE o.user_id = u.user_id AND o.status = 'paid' AND o.order_date < ?)",
                (cm, end)).fetchone()[0]
            vals.append(round(got * 100.0 / n, 1))
        out.append((cm, n, vals))
    return out


def fig_cohort_triangle():
    data = cohort_data()
    top = max(v for _, _, vs in data for v in vs if v is not None)
    lx, cw, rh, y0 = 58, 46, 26, 30
    h = y0 + rh * len(data) + 64
    svg = Svg("cohort-triangle", h,
              "Когортная таблица: доля купивших по месяцам жизни",
              "Строки — когорты по месяцу регистрации, столбцы — месяцы жизни от 0 до 5, в ячейке — процент когорты, "
              "купившей хотя бы раз к концу этого месяца. "
              f"Мартовская когорта дошла до {data[2][2][3]:g}%, январская остановилась на {data[0][2][1]:g}%. "
              "Правый нижний угол пуст: свежие когорты ещё не прожили эти месяцы.")
    svg.text(0, 14, "когорта", "f-hd", 12)
    for k in range(6):
        svg.text(lx + k * cw + cw / 2, 14, f"мес {k}", "f-sub", 10.5, anchor="middle")
    svg.line(0, y0 - 8, lx + 6 * cw, y0 - 8)
    for i, (cm, n, vals) in enumerate(data):
        y = y0 + i * rh
        svg.text(0, y + 12, cm, "f-t", 11.5)
        for k, v in enumerate(vals):
            if v is None:
                continue
            x = lx + k * cw
            op = 0.06 + 0.24 * v / top
            svg.rect(x + 1, y - 4, cw - 2, rh - 2, "f-tint", 3, f' fill-opacity="{op:.2f}"')
            svg.text(x + cw / 2, y + 12, f"{v:g}", "f-t", 11.5, anchor="middle")
    # стрелка к пустому углу
    ex, ey = lx + 5 * cw + cw / 2, y0 + 5 * rh + 6
    svg.path(f"M{ex - 70:g} {ey + 40:g} C {ex - 30:g} {ey + 40:g}, {ex:g} {ey + 30:g}, {ex:g} {ey + 12:g}")
    svg.note(0, y0 + rh * len(data) + 30, ["у свежих когорт хвоста ещё нет —", "это не падение"])
    return svg.render()
```

В `FIGS_M1` добавить `"cohort-triangle": fig_cohort_triangle,`. `checkfigs.js` не считает `fill-opacity` цветом.

- [ ] **Step 3: Проверка чисел проходит**

Run: `python3 инструменты/figs.py m1 --check`
Expected: `Числа сходятся с базой.`

- [ ] **Step 4: Метка в теории m1l6**

В `content-m1.js`, теория `m1l6`, раздел `<h3>Когорта: что это такое на самом деле</h3>` — сразу после блока «Как читать когортную таблицу» (его закрывающего `</div>`):

```html
<figure class="fig" data-fig="cohort-triangle"><figcaption>Рис. Та же конверсия, но по месяцам жизни когорты: так выглядит когортный «треугольник»</figcaption></figure>
```

- [ ] **Step 5: Сгенерировать, проверить, посмотреть**

Run: `python3 инструменты/figs.py m1 && node инструменты/checkfigs.js`
Expected: `схем 4`, `Ошибок нет.`
В браузере `#m1l6`: 390 и 1280 px, обе темы — числа читаются поверх заливки (в тёмной теме тоже), пустой угол справа внизу, стрелка указывает на него, реплика не налезает на таблицу.

- [ ] **Step 6: Коммит**

```bash
git add инструменты/figs.py content-m1.js
git commit -m "Схема «Когортный треугольник» в уроке 1.6

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Выпуск

**Files:**
- Modify: `sw.js` (`VERSION`), `Тетрадь аналитика.html` (сборка)

- [ ] **Step 1: Все проверки**

Run: `python3 инструменты/figs.py m1 --check && node инструменты/checkfigs.js && node инструменты/checkdrills.js && node инструменты/checkcards.js && node инструменты/checkterms.js`
Expected: везде «Ошибок нет» / «Числа сходятся с базой».

- [ ] **Step 2: Сквозной осмотр в браузере**

Уроки 1.2 (обе схемы), 1.3, 1.6 при 390 и 1280 px, светлая и тёмная тема: `document.documentElement.scrollWidth === innerWidth`; на 1280 px ширина `figure.fig` ≤ 520; подписи не обрезаны. Урок без схем (1.1) выглядит как раньше.

- [ ] **Step 3: Версия и сборка**

В `sw.js` поднять `VERSION` на `2026-09-25d` (на сайте `2026-09-25c`). Run: `python3 build.py`. Открыть `Тетрадь аналитика.html` в браузере — схема в уроке 1.2 на месте.

- [ ] **Step 4: Коммит, PR, выкладка**

```bash
git add sw.js "Тетрадь аналитика.html"
git commit -m "Схемы SQL: выпуск 2026-09-25d

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
git push -u origin stage3-figures
gh pr create --repo nekozlovskiy/analyst-notebook --base main --head stage3-figures \
  --title "Схемы в теории: порядок запроса, JOIN, окна, когорты"
```

Описание PR: что увидит ученик, чем проверено, `VERSION 2026-09-25d`, в конце строка «🤖 Generated with [Claude Code](https://claude.com/claude-code)». Слияние: попробовать влить самому; если фильтр не пускает — отдать пользователю команду `gh pr merge <N> --merge --repo nekozlovskiy/analyst-notebook && gh pr view <N> --repo nekozlovskiy/analyst-notebook --json state -q .state` для Run. После слияния дождаться `pages-build-deployment` и проверить `sw.js` и `content-m1.js` на сайте через `curl`.
