# Карточки «вопрос → ответ»: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в уроки модуля 1 карточки «вопрос → ответ», которые после первого прогона в уроке возвращаются в общее повторение на главной вместе с вопросами самопроверки.

**Architecture:** Карточки лежат полем `cards: [{q, a}]` в объекте урока в `content-m1.js`. Одну карточку рисует новый компонент `Flash.card` в `app.js`; им пользуются колода в уроке (`renderLesson`) и шаг повторения на главной (`Review.step`). Расписание — существующая корзина `review` в `Store`: ключ карточки `урок:cN`, ключ вопроса по-прежнему `урок:N`, в запись добавлен счётчик `miss`.

**Tech Stack:** обычный JS без сборки (ES2015, в `app.js` без стрелочных функций), CSS с токенами из `styles.css`, Node для скрипта проверки содержания, Python-сервер `инструменты/devserver.py` для проверки в браузере.

**Spec:** `docs/superpowers/specs/2026-09-17-cards-design.md`

## Global Constraints

- Язык интерфейса и карточек — русский. Тон: без восклицаний; тире в прозе допустимо.
- Один акцент — `var(--pen)`. Зелёный и красный — только для состояний; кнопка «Не вспомнил» без красного.
- Радиусы: контейнеры `var(--r-lg)` (13px), контролы `var(--r)` (7px). Других нет.
- Одна анимация на проект — `ink-in`.
- Правило коробок: рамку получает карточка (`.fc`), секция и итог колоды — без рамки.
- Хранилище: ключ `da.state.v1`, корзина `review`. Ключи вопросов `урок:номер` не меняются. Ключ карточки `урок:cномер`.
- Лестница `Review.STEPS` = `[1, 3, 7, 21]`, `Review.LIMIT` = 20 — без изменений.
- Кнопки в карточке не ниже 44px; на ширине меньше 600px кнопки оценки делят ряд пополам.
- `app.js` живёт внутри одной IIFE, стиль — `function`-выражения, `const`/`let`, без `=>`.
- Карточки: 6–12 на урок, `q` ≤ 160 знаков, `a` ≤ 400 знаков, из тегов только `<code>` и `<pre>`, числа из двух и больше цифр — только те, что есть в теории урока или в `ПРОГРЕСС.md`.
- В проекте нет автотестов. Логику проверяем в браузере сниппетами через `javascript_tool` на сервере `course-dev` (порт 8779), содержание — скриптом `node инструменты/checkcards.js`.

## Как проверять в браузере (для всех задач)

1. `preview_start` с `name: "course-dev"` — открывается http://localhost:8779.
2. Перед сниппетом со свежим кодом перезагрузить страницу с новым адресом, чтобы браузер не отдал старый `app.js`:
   ```js
   location.href = location.pathname + "?v=" + Date.now() + "#";
   ```
3. **Засев состояния.** `Store.flush` срабатывает на `beforeunload` и затёр бы записанное в `localStorage`, поэтому после записи глушим `setItem` на уходящей странице:
   ```js
   localStorage.setItem("da.state.v1", JSON.stringify(STATE));
   Storage.prototype.setItem = function () {};   /* уходящая страница не должна затереть засеянное */
   location.href = location.pathname + "?v=" + Date.now() + "#";
   ```
4. Даты для сниппетов (определять в каждом сниппете, где встречается `day`):
   ```js
   const day = function (n) { const d = new Date(); d.setDate(d.getDate() + n);
     return d.getFullYear() + "-" + String(d.getMonth() + 1).padStart(2, "0") + "-" + String(d.getDate()).padStart(2, "0"); };
   ```
5. `Store.set` пишет с задержкой 220 мс — перед чтением `localStorage` ждать 400 мс.

---

### Task 1: Скрипт проверки карточек и карточки урока 1.1

**Files:**
- Create: `инструменты/checkcards.js`
- Modify: `content-m1.js` — объект `window.CONTENT.m1l1`: поле `plan` (первая строка) и новое поле `cards` после `quiz`
- Modify: `content-core.js:18` — описание полей урока в шапке

**Interfaces:**
- Consumes: `window.COURSE` из `lessons.js`, `window.CONTENT` из `content-m1.js` … `content-m6.js`.
- Produces: `window.CONTENT.m1l1.cards` — массив из 9 объектов `{ q: string, a: string }`; команда `node инструменты/checkcards.js [--require <id модуля или урока>...]`, код выхода 1 при ошибках.

- [ ] **Step 1: Написать скрипт проверки**

Создать `инструменты/checkcards.js`:

```js
#!/usr/bin/env node
/* Проверка карточек «вопрос → ответ» в content-m1.js … content-m6.js.

     node инструменты/checkcards.js                  — формат всех карточек, какие есть
     node инструменты/checkcards.js --require m1     — и у каждого урока модуля 1 есть 6–12 карточек
     node инструменты/checkcards.js --require m1l2   — то же для одного урока

   Число из двух и больше цифр в ответе должно встречаться в теории
   урока или в ПРОГРЕСС.md: карточки не вводят непроверенных чисел.  */
"use strict";
const fs = require("fs");
const path = require("path");

const ROOT = path.join(__dirname, "..");
global.window = { CONTENT: {}, SH: new Proxy({}, { get: function () { return ""; } }) };
require(path.join(ROOT, "lessons.js"));
for (let n = 1; n <= 6; n++) require(path.join(ROOT, "content-m" + n + ".js"));

/* Числа сравниваются целиком: «17» не должно найтись внутри «17.09».
   Запятая и точка в дробях считаются одним и тем же. */
function numbers(text) {
  return (text.match(/\d+(?:[.,]\d+)?/g) || []).map(function (n) { return n.replace(",", "."); });
}
const progress = new Set(numbers(fs.readFileSync(path.join(ROOT, "ПРОГРЕСС.md"), "utf8")));
const args = process.argv.slice(2);
const required = args[0] === "--require" ? args.slice(1) : [];
const TAGS = { code: true, pre: true };
const errors = [];

function plain(html) {
  return html.replace(/<[^>]+>/g, "")
    .replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&amp;/g, "&");
}

function checkHtml(where, html) {
  const re = /<\/?([a-zA-Z0-9]+)[^>]*>/g;
  let m;
  while ((m = re.exec(html))) {
    if (!TAGS[m[1].toLowerCase()]) errors.push(where + ": тег <" + m[1] + "> не разрешён, можно только <code> и <pre>");
  }
  /* тот же диапазон, что в проверке после генерации текста из ПРОГРЕСС.md */
  Array.from(html).forEach(function (ch) {
    const c = ch.codePointAt(0);
    if (c > 0x2E00 && c < 0xFF00) errors.push(where + ": посторонний символ «" + ch + "»");
  });
}

let total = 0;
window.COURSE.modules.forEach(function (m) {
  m.lessons.forEach(function (l) {
    const C = window.CONTENT[l.id];
    const cards = C && C.cards;
    const need = required.indexOf(m.id) >= 0 || required.indexOf(l.id) >= 0;
    if (cards === undefined) {
      if (need) errors.push(l.id + ": нет карточек");
      return;
    }
    if (!Array.isArray(cards)) { errors.push(l.id + ": cards должно быть массивом"); return; }
    if (need && (cards.length < 6 || cards.length > 12)) {
      errors.push(l.id + ": карточек " + cards.length + ", нужно от 6 до 12");
    }
    const theory = new Set(numbers(plain(C.theory || "")));
    cards.forEach(function (c, i) {
      const where = l.id + " карточка " + i;
      if (!c || typeof c.q !== "string" || !c.q.trim()) { errors.push(where + ": пустой вопрос q"); return; }
      if (typeof c.a !== "string" || !c.a.trim()) { errors.push(where + ": пустой ответ a"); return; }
      if (/<pre/i.test(c.q)) errors.push(where + ": в вопросе нельзя <pre>");
      checkHtml(where, c.q);
      checkHtml(where, c.a);
      if (plain(c.q).length > 160) errors.push(where + ": вопрос длиннее 160 знаков");
      if (plain(c.a).length > 400) errors.push(where + ": ответ длиннее 400 знаков");
      numbers(plain(c.a)).forEach(function (num) {
        if (num.replace(/\D/g, "").length < 2) return;
        if (!theory.has(num) && !progress.has(num)) {
          errors.push(where + ": число " + num + " не найдено ни в теории урока, ни в ПРОГРЕСС.md");
        }
      });
    });
    total += cards.length;
    console.log(l.id + "  " + cards.length);
  });
});

if (errors.length) {
  console.error("\n" + errors.join("\n") + "\n\nОшибок: " + errors.length);
  process.exit(1);
}
console.log("\nВсего карточек: " + total + ". Ошибок нет.");
```

- [ ] **Step 2: Убедиться, что проверка падает без карточек**

Run: `node инструменты/checkcards.js --require m1l1`
Expected: FAIL, код выхода 1, в выводе `m1l1: нет карточек` и `Ошибок: 1`.

- [ ] **Step 3: Добавить карточки урока 1.1**

В `content-m1.js`, в объекте `window.CONTENT.m1l1`, сразу после массива `quiz` (перед `links`) вставить:

```js
  cards: [
    { q: "Чем LEFT JOIN отличается от INNER JOIN?",
      a: "INNER оставляет только строки, у которых нашлась пара с обеих сторон. LEFT оставляет все строки левой таблицы, а где пары нет — справа ставит NULL." },
    { q: "Почему <code>COUNT(*)</code> после JOIN может завысить число пользователей?",
      a: "JOIN «один ко многим» повторяет пользователя столько раз, сколько у него заказов. <code>COUNT(*)</code> считает строки после соединения, а людей считают через <code>COUNT(DISTINCT u.user_id)</code>." },
    { q: "После LEFT JOIN у канала нет ни одного заказа. Что вернут <code>COUNT(o.order_id)</code> и <code>COUNT(*)</code>?",
      a: "<code>COUNT(o.order_id)</code> пропускает NULL и даёт честный 0. <code>COUNT(*)</code> считает саму строку с NULL и даёт 1." },
    { q: "Чем опасен JOIN двух таблиц «многие ко многим», например заказов и событий?",
      a: "Строки перемножаются: 3 заказа и 5 событий дают 15 строк, и <code>SUM(revenue)</code> завысит выручку в пять раз. Каждую таблицу сначала агрегируют до строки на пользователя и только потом соединяют." },
    { q: "Что случится, если после LEFT JOIN поставить условие по правой таблице в WHERE?",
      a: "Соединение молча превратится в INNER: у строк без пары справа NULL, условие даёт «неизвестно», и WHERE их отбрасывает. Фильтр по правой таблице ставят в ON, по левой — в WHERE." },
    { q: "Почему в WHERE нельзя написать <code>SUM(revenue) &gt; 100</code>?",
      a: "WHERE выполняется до группировки — агрегатов на этом шаге ещё нет. Условие на агрегат пишут в HAVING." },
    { q: "Почему алиас из SELECT не виден в WHERE, но виден в ORDER BY?",
      a: "SELECT выполняется почти последним: после WHERE, но до ORDER BY. Когда работает WHERE, алиаса ещё не существует." },
    { q: "Чему равно <code>NULL = NULL</code> и как проверить значение на NULL?",
      a: "Не TRUE: NULL значит «значение неизвестно», и сравнение двух неизвестных тоже неизвестно. Проверяют через <code>IS NULL</code>." },
    { q: "Что вернёт <code>SUM(revenue)</code> по пустому набору строк и как получить 0?",
      a: "NULL, а не 0. Закрывают через <code>COALESCE(SUM(revenue), 0)</code>." }
  ],
```

- [ ] **Step 4: Поправить строку теории в плане урока 1.1**

В `window.CONTENT.m1l1.plan` заменить

```js
    { m: "30 мин", w: "Теория: четыре вида соединений и две главные ловушки" },
```

на

```js
    { m: "35 мин", w: "Теория и карточки: четыре вида соединений и две главные ловушки" },
```

- [ ] **Step 5: Описать поле в шапке `content-core.js`**

После строки `     quiz       — [{q, opts, right, why}] — самопроверка` добавить:

```
     cards      — [{q, a}] — карточки «вопрос → ответ», 6–12 на урок;
                  проверка: node инструменты/checkcards.js --require <урок>
```

- [ ] **Step 6: Убедиться, что проверка проходит**

Run: `node инструменты/checkcards.js --require m1l1`
Expected: PASS, код выхода 0, строка `m1l1  9` и `Всего карточек: 9. Ошибок нет.`

- [ ] **Step 7: Commit**

```bash
git add инструменты/checkcards.js content-m1.js content-core.js
git commit -m "Карточки урока 1.1 и скрипт их проверки

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Компонент карточки и смешанное повторение на главной

**Files:**
- Modify: `app.js:736-876` — блок «Повторение»: новый объект `Flash` перед `Review`, `Review` целиком заменён
- Modify: `styles.css:1233` — после правила `.quiz-score b` новый раздел стилей карточки
- Modify: `app.js:2430-2437` — `Keys.list`

**Interfaces:**
- Consumes: `window.CONTENT.m1l1.cards` (Task 1); утилиты `$`, `el`, `esc`, `plural`, `shuffled`, `isoDay`, `addDays`, `daysBetween`; `Store`, `Course`, `Lazy`, `Router`.
- Produces:
  - `Flash.card(box: Element, card: {q, a}, meta: string /* HTML */, onRate: function(ok: boolean)): Element` — рисует карточку в `box`, возвращает корневой `.fc`. Классы: `.fc`, `.fc-meta`, `.fc-q`, `.fc-a`, `.fc-act`, `.fc-show`, `.fc-no`, `.fc-yes`; на корне после открытия `.open`, после оценки `.yes` или `.no`.
  - `Review.parse(key: string): {key, id, kind: "quiz"|"card", n: number} | null`
  - `Review.record(id: string, ref: number|string, ok: boolean, fromLesson: boolean): boolean` — `ref` = номер вопроса или `"c" + номер карточки`; `false`, если запись пропущена.
  - `Review.items()` / `Review.due()` — элементы `{key, id, kind, n, r}` (поля `qi` больше нет).
  - `Review.say(list): string` — «12: 5 вопросов и 7 карточек» / «7 карточек» / «5 вопросов».

- [ ] **Step 1: Проверка, которая должна упасть**

Открыть `course-dev`. Засеять состояние (см. «Как проверять в браузере»), где `STATE`:

```js
const STATE = { theme: {}, done: { m1l1: Date.now() }, review: {
  "m1l1:0":  { step: 0, due: day(-1) },
  "m1l1:1":  { step: 0, due: day(-1) },
  "m1l1:c0": { step: 0, due: day(-1) },
  "m1l1:c1": { step: 1, due: day(-1), miss: 2 }
} };
```

После перезагрузки:

```js
document.querySelector(".review-intro").textContent
```

Expected до реализации: `"Сегодня 4 вопроса из пройденных уроков. Минут пять — и материал останется с вами надолго."` — неверно, это и есть падение. После реализации должно быть `"Сегодня 4: 2 вопроса и 2 карточки из пройденных уроков. Минут пять — и материал останется с вами надолго."`

- [ ] **Step 2: Заменить блок «Повторение» в `app.js`**

Заменить весь фрагмент от комментария `/* ====… Повторение` (строка 736) до закрывающей `};` объекта `Review` (строка 876) на:

```js
/* ============================================================
   Карточка «вопрос → ответ»

   Одна карточка в box: вопрос и «Показать ответ», затем ответ под
   чертой и две оценки. onRate(ok) вызывается ровно один раз. Какую
   карточку показать следующей и писать ли оценку в расписание —
   решает тот, кто вызвал: колода в уроке или повторение на главной.
   ============================================================ */

const Flash = {
  card: function (box, card, meta, onRate) {
    box.innerHTML =
      '<div class="fc">' +
        '<div class="fc-meta">' + meta + "</div>" +
        '<div class="fc-q">' + card.q + "</div>" +
        '<div class="fc-a" aria-live="polite"></div>' +
        '<div class="fc-act">' +
          '<button class="btn primary fc-show" type="button">Показать ответ</button>' +
        "</div>" +
      "</div>";
    const root = $(".fc", box), act = $(".fc-act", root), ans = $(".fc-a", root);
    let rated = false;

    function show() {
      if (root.classList.contains("open")) return;
      root.classList.add("open");
      ans.innerHTML = card.a;
      act.innerHTML =
        '<button class="btn fc-no" type="button">Не вспомнил</button>' +
        '<button class="btn primary fc-yes" type="button">Вспомнил</button>';
      $(".fc-no", act).addEventListener("click", function () { rate(false); });
      $(".fc-yes", act).addEventListener("click", function () { rate(true); });
      $(".fc-yes", act).focus({ preventScroll: true });
    }
    function rate(ok) {
      if (rated) return;
      rated = true;
      root.classList.add(ok ? "yes" : "no");
      Array.prototype.forEach.call(act.querySelectorAll("button"), function (b) { b.disabled = true; });
      onRate(ok);
    }

    $(".fc-show", act).addEventListener("click", show);
    /* Пробел и Enter нажимают кнопку в фокусе сами — «Показать ответ»,
       потом «Вспомнил». Стрелки — оценка, когда ответ уже открыт. */
    root.addEventListener("keydown", function (e) {
      if (e.metaKey || e.ctrlKey || e.altKey || rated || !root.classList.contains("open")) return;
      if (e.key === "ArrowLeft") { e.preventDefault(); rate(false); }
      else if (e.key === "ArrowRight") { e.preventDefault(); rate(true); }
    });
    return root;
  }
};

/* ============================================================
   Повторение

   Вопрос из самопроверки и карточка из урока возвращаются через 1, 3,
   7 и 21 день. Верный ответ или «вспомнил» переводит на ступень дальше,
   ошибка — снова на завтра. Верный ответ на последней ступени — выучено,
   из очереди уходит. Хранится в Store: «урок:номер» — вопрос,
   «урок:cномер» — карточка; miss — сколько раз ошибся или не вспомнил.
   ============================================================ */

const Review = {
  STEPS: [1, 3, 7, 21],
  LIMIT: 20,              /* больше за раз — уже не пять минут, а урок */

  parse: function (k) {
    const m = /^([^:]+):(c?)(\d+)$/.exec(k);
    return m ? { key: k, id: m[1], kind: m[2] ? "card" : "quiz", n: +m[3] } : null;
  },

  /* ref — номер вопроса или "c" + номер карточки. false — запись
     пропущена: повторный проход в уроке не должен сбивать расписание. */
  record: function (id, ref, ok, fromLesson) {
    const k = id + ":" + ref;
    const cur = Store.get("review", k, null);
    if (fromLesson && cur) return false;
    const step = cur ? cur.step : -1;
    const miss = ((cur && cur.miss) || 0) + (ok ? 0 : 1);
    if (ok && step + 1 >= Review.STEPS.length) {
      Store.set("review", k, { step: step, done: true, miss: miss });
      return true;
    }
    const next = ok ? step + 1 : 0;
    Store.set("review", k, { step: next, due: addDays(isoDay(), Review.STEPS[next]), miss: miss });
    return true;
  },

  items: function () {
    const all = Store.all().review || {};
    return Object.keys(all).map(function (k) {
      const x = Review.parse(k);
      if (x) x.r = all[k];
      return x;
    }).filter(function (x) {
      return x && x.r && !x.r.done && x.r.due && Course.byId(x.id);
    });
  },
  due: function () {
    const t = isoDay();
    return Review.items().filter(function (x) { return x.r.due <= t; });
  },
  nextDate: function () {
    const t = isoDay();
    return Review.items().map(function (x) { return x.r.due; })
      .filter(function (d) { return d > t; }).sort()[0] || null;
  },
  when: function (iso) {
    const n = daysBetween(isoDay(), iso);
    if (n <= 1) return "завтра";
    if (n === 2) return "послезавтра";
    return "через " + n + " " + plural(n, "день", "дня", "дней");
  },

  /* «12: 5 вопросов и 7 карточек», а если вид один — «7 карточек» */
  say: function (list) {
    const c = list.filter(function (x) { return x.kind === "card"; }).length;
    const q = list.length - c;
    const qs = q + " " + plural(q, "вопрос", "вопроса", "вопросов");
    const cs = c + " " + plural(c, "карточка", "карточки", "карточек");
    return q && c ? list.length + ": " + qs + " и " + cs : c ? cs : qs;
  },

  /* Блок на главной. Пока в очереди ничего нет, его нет вовсе. */
  section: function () {
    const due = Review.due(), next = Review.nextDate();
    if (!due.length && !next) return null;
    const n = Math.min(due.length, Review.LIMIT);
    const node = el("section", { class: "review has-margin" });
    node.innerHTML =
      '<div class="aside"><p>вспомнить с усилием — это и есть запоминание</p></div>' +
      '<div class="sec-title">Повторение</div>' +
      '<div class="review-body">' +
      (due.length
        ? '<p class="review-intro">' +
            (due.length > n
              ? "Накопилось " + Review.say(due) + ", сегодня возьмём " + n + ". "
              : "Сегодня " + Review.say(due) + " из пройденных уроков. ") +
            "Минут пять — и материал останется с вами надолго.</p>" +
          '<button class="btn primary" id="rvStart" type="button">Начать повторение</button>'
        : '<p class="review-later">Всё повторено. Следующие вопросы вернутся ' + Review.when(next) + ".</p>") +
      "</div>";
    const start = $("#rvStart", node);
    if (start) start.addEventListener("click", function () { Review.run($(".review-body", node)); });
    return node;
  },

  run: function (box) {
    const list = Review.due();
    const order = shuffled(list.length).slice(0, Review.LIMIT).map(function (i) { return list[i]; });
    const mods = {};
    order.forEach(function (x) { mods[Course.byId(x.id).module.id] = true; });
    box.innerHTML = '<p class="review-intro">Достаю вопросы…</p>';
    Promise.all(Object.keys(mods).map(function (m) { return Lazy.content(m); })).then(function () {
      /* урок могли переписать — вопроса или карточки с таким номером может уже не быть */
      const ok = order.filter(function (x) {
        const C = window.CONTENT[x.id];
        const items = C && (x.kind === "card" ? C.cards : C.quiz);
        return items && items[x.n];
      });
      Review.step(box, ok, 0, 0);
    }, function () {
      box.innerHTML = '<p class="review-intro">Вопросы не загрузились — похоже, пропал интернет.</p>' +
        '<button class="btn" id="rvRetry" type="button">Попробовать ещё раз</button>';
      $("#rvRetry", box).addEventListener("click", function () { Review.run(box); });
    });
  },

  step: function (box, order, i, right) {
    if (i >= order.length) {
      const next = Review.nextDate();
      box.innerHTML = '<p class="review-intro">Готово: верно ' + right + " из " + order.length + ". " +
        (next ? "Следующие вопросы вернутся " + Review.when(next) + "." : "Все вопросы выучены.") + "</p>" +
        '<button class="btn" id="rvClose" type="button">Закрыть</button>';
      $("#rvClose", box).addEventListener("click", function () { Router.render(true); });
      return;
    }
    const x = order[i], L = Course.byId(x.id), C = window.CONTENT[x.id];
    const from = " из " + order.length + ", из урока " + L.num + " «" + esc(L.title) + "»";
    const nextHtml = '<div class="rv-next" hidden><button class="btn primary" type="button">' +
      (i + 1 < order.length ? "Дальше" : "Закончить") + "</button></div>";

    /* после ответа — кнопка дальше, фокус на неё: можно идти с клавиатуры */
    function next(ok) {
      const nx = $(".rv-next", box);
      nx.hidden = false;
      const nb = $("button", nx);
      nb.addEventListener("click", function () { Review.step(box, order, i + 1, right + (ok ? 1 : 0)); });
      nb.focus();
    }

    if (x.kind === "card") {
      box.innerHTML = '<div class="rv-card"></div>' + nextHtml;
      const root = Flash.card($(".rv-card", box), C.cards[x.n], "Карточка " + (i + 1) + from, function (ok) {
        Review.record(x.id, "c" + x.n, ok, false);
        next(ok);
      });
      $(".fc-show", root).focus({ preventScroll: true });
      return;
    }

    const q = C.quiz[x.n];
    let h = '<div class="rv-meta">Вопрос ' + (i + 1) + from + "</div>" +
      '<div class="q"><div class="q-t"><span>' + q.q + '</span></div><div class="q-opts">';
    shuffled(q.opts.length).forEach(function (orig, pos) {
      h += '<button class="q-opt" type="button" data-i="' + orig + '">' +
        '<span class="mk">' + "АБВГД".charAt(pos) + "</span><span>" + q.opts[orig] + "</span></button>";
    });
    h += '</div><div class="q-why"><b>Почему:</b> ' + q.why + "</div></div>" + nextHtml;
    box.innerHTML = h;

    const opts = Array.prototype.slice.call(box.querySelectorAll(".q-opt"));
    opts.forEach(function (btn) {
      btn.addEventListener("click", function () {
        if (btn.disabled) return;
        const picked = +btn.dataset.i, ok = picked === q.right;
        Review.record(x.id, x.n, ok, false);
        opts.forEach(function (b) {
          b.disabled = true;
          const oi = +b.dataset.i;
          if (oi === q.right) b.classList.add("right");
          else if (oi === picked) b.classList.add("wrong");
        });
        $(".q-why", box).classList.add("show");
        next(ok);
      });
    });
  }
};
```

- [ ] **Step 3: Стили карточки**

В `styles.css` сразу после правила `.quiz-score b { … }` (строка 1233) вставить:

```css
/* ============================================================
   Карточки «вопрос → ответ»
   Карточка — отдельный объект, поэтому в рамке. Ответ проявляется
   на месте под чертой той же анимацией, что и разбор вопроса.
   ============================================================ */

.deck { max-width: 680px; }
.fc {
  background: var(--card); border: 1px solid var(--rule); border-radius: var(--r-lg);
  padding: 20px 22px 18px;
}
.fc-meta { font-size: 13.5px; color: var(--ink-3); margin-bottom: 10px; }
.fc-q { font-size: 17px; font-weight: 500; line-height: 1.5; color: var(--ink); }
.fc-a {
  margin-top: 16px; padding-top: 16px; border-top: 1px solid var(--rule);
  font-family: var(--serif); font-size: 16.5px; line-height: 1.62; color: var(--ink-2);
  animation: ink-in .26s var(--ease) both;
}
.fc-a:empty { display: none; }
.fc-q code, .fc-a code {
  font-family: var(--mono); font-size: .86em; background: var(--paper-2);
  padding: 2px 5px; border-radius: var(--r);
}
.fc-a pre {
  margin: 10px 0 0; padding: 10px 12px; background: var(--paper-2); border-radius: var(--r);
  font-family: var(--mono); font-size: 14px; line-height: 1.55; overflow-x: auto;
}
.fc-a pre code { background: none; padding: 0; font-size: inherit; }
.fc-act { display: flex; gap: 10px; margin-top: 18px; }
.fc-act .btn { min-height: 44px; }
/* после оценки выбранная кнопка не бледнеет — видно, что отмечено */
.fc.yes .fc-yes:disabled, .fc.no .fc-no:disabled { opacity: 1; }
.fc-done p {
  font-family: var(--serif); font-size: 17px; line-height: 1.6; color: var(--ink-2);
  margin: 0 0 14px;
}

@media (max-width: 600px) {
  .fc { padding: 16px; }
  .fc-act .btn { flex: 1 1 0; justify-content: center; }
}
```

- [ ] **Step 4: Строка в окне «Клавиши»**

В `Keys.list` после строки `[["Cmd", "&crarr;"], …]` добавить:

```js
    [["Пробел", "&larr;", "&rarr;"], "Карточка: открыть ответ, не вспомнил, вспомнил", "в уроке и в повторении"],
```

- [ ] **Step 5: Проверка в браузере — подводка и прохождение**

Перезагрузить `course-dev` с новым адресом, засеять `STATE` из Step 1. Прочитать подводку:

```js
document.querySelector(".review-intro").textContent
```

Expected: `"Сегодня 4: 2 вопроса и 2 карточки из пройденных уроков. Минут пять — и материал останется с вами надолго."`

Пройти очередь: карточки — «вспомнил», в вопросах — первый вариант:

```js
const day = function (n) { const d = new Date(); d.setDate(d.getDate() + n);
  return d.getFullYear() + "-" + String(d.getMonth() + 1).padStart(2, "0") + "-" + String(d.getDate()).padStart(2, "0"); };
const wait = function (ms) { return new Promise(function (r) { setTimeout(r, ms); }); };
document.getElementById("rvStart").click();
await wait(1500);
const box = document.querySelector(".review-body");
const log = []; let quizRight = 0;
for (let k = 0; k < 10; k++) {
  if (box.querySelector(".fc")) {
    log.push(box.querySelector(".fc-meta").textContent);
    box.querySelector(".fc-show").click();
    log.push("ответ открыт: " + (box.querySelector(".fc-a").textContent.length > 0));
    box.querySelector(".fc-yes").click();
  } else if (box.querySelector(".q-opt")) {
    log.push(box.querySelector(".rv-meta").textContent);
    const b = box.querySelector(".q-opt"); b.click();
    if (b.classList.contains("right")) quizRight++;
  } else break;
  box.querySelector(".rv-next button").click();
}
await wait(400);
const r = JSON.parse(localStorage.getItem("da.state.v1")).review;
({ log: log, final: box.querySelector(".review-intro").textContent, quizRight: quizRight,
   c1: r["m1l1:c1"], c0: r["m1l1:c0"], expectC1due: day(7), expectC0due: day(3) });
```

Expected:
- в `log` четыре подписи: две вида `"Карточка N из 4, из урока 1.1 «JOIN: соединяем таблицы»"`, две вида `"Вопрос N из 4, из урока 1.1 «JOIN: соединяем таблицы»"`; после каждой карточки `"ответ открыт: true"`;
- `final` начинается с `"Готово: верно " + (2 + quizRight) + " из 4."`;
- `c1` = `{ step: 2, due: expectC1due, miss: 2 }`;
- `c0` = `{ step: 1, due: expectC0due, miss: 0 }`.

- [ ] **Step 6: Проверка в браузере — «не вспомнил», клавиши и лимит**

Засеять состояние с одной карточкой:

```js
const STATE = { theme: {}, done: { m1l1: Date.now() }, review: { "m1l1:c2": { step: 2, due: day(-1) } } };
```

После перезагрузки нажать `#rvStart`, подождать 1500 мс. Фокус уже на «Показать ответ»:

```js
document.activeElement.className
```

Expected: `"btn primary fc-show"`.

Клавиатурой через `computer`: `key` = `space`. Затем:

```js
[document.querySelector(".fc-a").textContent.length > 0, document.activeElement.className]
```

Expected: `[true, "btn primary fc-yes"]`.

`computer` `key` = `Left`. Подождать 400 мс:

```js
const day = function (n) { const d = new Date(); d.setDate(d.getDate() + n);
  return d.getFullYear() + "-" + String(d.getMonth() + 1).padStart(2, "0") + "-" + String(d.getDate()).padStart(2, "0"); };
[document.querySelector(".rv-next").hidden, document.activeElement.textContent,
 JSON.parse(localStorage.getItem("da.state.v1")).review["m1l1:c2"], day(1)]
```

Expected: `[false, "Закончить", { step: 0, due: <day(1)>, miss: 1 }, <day(1)>]`.

Лимит: засеять 27 карточных ключей — номера `c0…c8` у уроков 1.1, 1.2 и 1.3. У 1.2 и 1.3 карточек пока нет: в подводке они считаются, в прогоне отфильтруются — для проверки подводки это не важно.

```js
const review = {};
["m1l1", "m1l2", "m1l3"].forEach(function (id) { for (let i = 0; i < 9; i++) review[id + ":c" + i] = { step: 0, due: day(-1) }; });
const STATE = { theme: {}, done: { m1l1: Date.now() }, review: review };
```

После перезагрузки:

```js
document.querySelector(".review-intro").textContent
```

Expected: `"Накопилось 27 карточек, сегодня возьмём 20. Минут пять — и материал останется с вами надолго."`

Окно клавиш: `computer` `key` = `shift+slash`, затем `document.querySelector(".modal .keys").textContent`. Expected: содержит `"Карточка: открыть ответ, не вспомнил, вспомнил"`. Закрыть: `key` = `Escape`.

- [ ] **Step 7: Регрессия — вопросы в уроке и старые записи**

Засеять старое состояние без `miss`:

```js
const STATE = { theme: {}, done: { m1l1: Date.now() }, review: { "m1l1:3": { step: 1, due: day(-1) } } };
```

После перезагрузки подводка — `"Сегодня 1 вопрос из пройденных уроков. Минут пять — и материал останется с вами надолго."`. Пройти его, выбрав неверный вариант:

```js
const wait = function (ms) { return new Promise(function (r) { setTimeout(r, ms); }); };
document.getElementById("rvStart").click(); await wait(1500);
const q = window.CONTENT.m1l1.quiz[3];
Array.prototype.find.call(document.querySelectorAll(".review-body .q-opt"), function (b) { return +b.dataset.i !== q.right; }).click();
await wait(400);
JSON.parse(localStorage.getItem("da.state.v1")).review["m1l1:3"]
```

Expected: `{ step: 0, due: <day(1)>, miss: 1 }`.

Вопрос в уроке: засеять `{ theme: {}, done: {}, review: {} }`, открыть `#m1l1`, подождать 1000 мс, ответить неверно на вопрос 0:

```js
const wait = function (ms) { return new Promise(function (r) { setTimeout(r, ms); }); };
const q = window.CONTENT.m1l1.quiz[0];
Array.prototype.find.call(document.querySelectorAll('.q[data-q="0"] .q-opt'), function (b) { return +b.dataset.i !== q.right; }).click();
await wait(400);
JSON.parse(localStorage.getItem("da.state.v1")).review["m1l1:0"]
```

Expected: `{ step: 0, due: <day(1)>, miss: 1 }`.

`read_console_messages` с `onlyErrors: true` — Expected: ошибок нет.

- [ ] **Step 8: Commit**

```bash
git add app.js styles.css
git commit -m "Карточка и повторение карточек на главной

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Колода карточек в уроке

**Files:**
- Modify: `app.js` — `renderLesson`: флаг `hasCards` (рядом с `hasQuiz`), якорная навигация (`const secs = …`), HTML секции (перед комментарием `/* ---------- тренажёр ---------- */`), вставка секции между теорией и задачей в `main.innerHTML`, запуск колоды (перед комментарием `/* ---------- эталон ---------- */`)
- Modify: `styles.css` — правило в `@media print` (раздел «17. Доступность и печать»)

**Interfaces:**
- Consumes: `Flash.card(box, card, meta, onRate): Element` и `Review.record(id, ref, ok, fromLesson): boolean` из Task 2; `window.CONTENT[id].cards` из Task 1.
- Produces: секция `#s-cards` с контейнером `#deck`; итог `.fc-done` с кнопкой `#deckAgain`.

- [ ] **Step 1: Проверка, которая должна упасть**

Засеять `{ theme: {}, done: {}, review: {} }`, открыть `#m1l1`, подождать 1000 мс:

```js
[!!document.getElementById("s-cards"), Array.prototype.map.call(document.querySelectorAll(".secnav a"), function (a) { return a.textContent; })]
```

Expected до реализации: `[false, ["Теория","Задача","Тренажёр","Самопроверка","Что почитать","Заметки"]]` — падение. После: `[true, ["Теория","Карточки","Задача","Тренажёр","Самопроверка","Что почитать","Заметки"]]`.

- [ ] **Step 2: Флаг и навигация**

После строки `const hasQuiz = !!(C.quiz && C.quiz.length);` добавить:

```js
  const hasCards = !!(C.cards && C.cards.length);
```

Заменить

```js
  const secs = [{ id: "s-theory", t: "Теория" }, { id: "s-task", t: "Задача" }];
```

на

```js
  const secs = [{ id: "s-theory", t: "Теория" }];
  if (hasCards) secs.push({ id: "s-cards", t: "Карточки" });
  secs.push({ id: "s-task", t: "Задача" });
```

- [ ] **Step 3: HTML секции**

Перед комментарием `/* ---------- тренажёр ---------- */` вставить:

```js
  /* ---------- карточки ---------- */
  /* Сразу после теории: прочитал — вспомнил без подсказки — применил в задаче. */
  let cardsHtml = "";
  if (hasCards) {
    cardsHtml = '<section class="block" id="s-cards">' +
      '<div class="block-h"><h2>Карточки</h2></div>' +
      '<p class="block-intro">Не подглядывая в теорию: сначала ответьте в голове, потом откройте ответ ' +
      "и честно оцените себя. С завтрашнего дня карточки будут возвращаться в повторение на главной.</p>" +
      '<div class="deck" id="deck"></div></section>';
  }
```

В сборке `main.innerHTML` заменить

```js
      '<div class="theory">' + C.theory + "</div>" +
    "</section>" +

    '<section class="block has-margin" id="s-task">' +
```

на

```js
      '<div class="theory">' + C.theory + "</div>" +
    "</section>" +

    cardsHtml +

    '<section class="block has-margin" id="s-task">' +
```

- [ ] **Step 4: Колода**

Перед комментарием `/* ---------- эталон ---------- */` вставить:

```js
  /* ---------- колода карточек ---------- */
  /* Первый прогон ставит карточки в расписание повторения (запись
     пропускается, если карточка там уже есть). Прогон «ещё раз» только
     закрепляет и расписание не трогает. */
  if (hasCards) {
    const deck = $("#deck");
    const every = C.cards.map(function (c, k) { return k; });
    const runDeck = function (list, record) {
      let i = 0, yes = 0, fresh = 0;
      const missed = [];
      const finish = function () {
        const tail = !record ? "Расписание повторения этот прогон не меняет."
          : fresh ? "Завтра карточки вернутся в повторение на главной."
          : "Эти карточки уже в расписании повторения, прогон его не сдвинул.";
        deck.innerHTML = '<div class="fc-done"><p>Вспомнили ' + yes + " из " + list.length + ". " + tail + "</p>" +
          '<button class="btn" id="deckAgain" type="button">' +
          (missed.length ? "Ещё раз невспомненные: " + missed.length : "Прогнать ещё раз") + "</button></div>";
        $("#deckAgain").addEventListener("click", function () {
          runDeck(missed.length ? missed : every, false);
          $(".fc-show", deck).focus({ preventScroll: true });
        });
      };
      const show = function () {
        if (i >= list.length) { finish(); return; }
        const n = list[i];
        Flash.card(deck, C.cards[n], (i + 1) + " из " + list.length, function (ok) {
          if (record && Review.record(id, "c" + n, ok, true)) fresh++;
          if (ok) yes++; else missed.push(n);
          i++;
          show();
          /* фокус на следующую кнопку, чтобы колоду можно было пройти с клавиатуры */
          const b = $(".fc-show", deck) || $("#deckAgain");
          if (b) b.focus({ preventScroll: true });
        });
      };
      show();
    };
    runDeck(every, true);
  }
```

- [ ] **Step 5: Печать**

В `styles.css`, в `@media print`, заменить

```css
  .hdr, .secnav, .readbar, .actions, .lesson-nav, .toast, .warnbar,
  .find, .modal-bg { display: none !important; }
```

на

```css
  .hdr, .secnav, .readbar, .actions, .lesson-nav, .toast, .warnbar,
  .find, .modal-bg, #s-cards { display: none !important; }
```

- [ ] **Step 6: Проверка в браузере — первый прогон**

Перезагрузить с новым адресом, засеять `{ theme: {}, done: {}, review: {} }`, открыть `#m1l1`, подождать 1000 мс. Повторить проверку из Step 1 — Expected: `[true, ["Теория","Карточки","Задача","Тренажёр","Самопроверка","Что почитать","Заметки"]]`.

```js
const day = function (n) { const d = new Date(); d.setDate(d.getDate() + n);
  return d.getFullYear() + "-" + String(d.getMonth() + 1).padStart(2, "0") + "-" + String(d.getDate()).padStart(2, "0"); };
const wait = function (ms) { return new Promise(function (r) { setTimeout(r, ms); }); };
const deck = document.getElementById("deck");
const total = window.CONTENT.m1l1.cards.length;
const res = { total: total, first: deck.querySelector(".fc-meta").textContent };
deck.querySelector(".fc-show").click();
deck.querySelector(".fc-no").click();
res.second = deck.querySelector(".fc-meta").textContent;
for (let k = 1; k < total; k++) { deck.querySelector(".fc-show").click(); deck.querySelector(".fc-yes").click(); }
res.done = deck.querySelector(".fc-done p").textContent;
res.again = document.getElementById("deckAgain").textContent;
await wait(400);
const r = JSON.parse(localStorage.getItem("da.state.v1")).review;
res.c0 = r["m1l1:c0"]; res.c1 = r["m1l1:c1"]; res.tomorrow = day(1);
res
```

Expected:
- `total` = 9, `first` = `"1 из 9"`, `second` = `"2 из 9"`;
- `done` = `"Вспомнили 8 из 9. Завтра карточки вернутся в повторение на главной."`;
- `again` = `"Ещё раз невспомненные: 1"`;
- `c0` = `{ step: 0, due: tomorrow, miss: 1 }`, `c1` = `{ step: 0, due: tomorrow, miss: 0 }`.

- [ ] **Step 7: Проверка в браузере — «ещё раз» и повторное открытие урока**

```js
const wait = function (ms) { return new Promise(function (r) { setTimeout(r, ms); }); };
const deck = document.getElementById("deck");
document.getElementById("deckAgain").click();
const meta = deck.querySelector(".fc-meta").textContent;
deck.querySelector(".fc-show").click(); deck.querySelector(".fc-yes").click();
await wait(400);
[meta, deck.querySelector(".fc-done p").textContent, document.getElementById("deckAgain").textContent,
 JSON.parse(localStorage.getItem("da.state.v1")).review["m1l1:c0"]]
```

Expected: `["1 из 1", "Вспомнили 1 из 1. Расписание повторения этот прогон не меняет.", "Прогнать ещё раз", { step: 0, due: <day(1)>, miss: 1 }]`.

Открыть урок заново: `location.href = location.pathname + "?v=" + Date.now() + "#m1l1";`, подождать 1000 мс, пройти все карточки «вспомнил»:

```js
const deck = document.getElementById("deck");
const total = window.CONTENT.m1l1.cards.length;
for (let k = 0; k < total; k++) { deck.querySelector(".fc-show").click(); deck.querySelector(".fc-yes").click(); }
[deck.querySelector(".fc-done p").textContent, JSON.parse(localStorage.getItem("da.state.v1")).review["m1l1:c0"]]
```

Expected: `["Вспомнили 9 из 9. Эти карточки уже в расписании повторения, прогон его не сдвинул.", { step: 0, due: <day(1)>, miss: 1 }]`.

- [ ] **Step 8: Проверка в браузере — клавиши, урок без карточек, телефон, тёмная тема**

Клавиши. Открыть `#m1l1` заново (новый адрес), подождать 1000 мс:

```js
document.querySelector("#deck .fc-show").scrollIntoView({ block: "center" });
document.querySelector("#deck .fc-show").focus();
```

`computer` `key` = `space`, затем `key` = `Left`. Затем:

```js
[document.querySelector("#deck .fc-meta").textContent, document.activeElement.className]
```

Expected: `["2 из 9", "btn primary fc-show"]`.

Стрелки в заметках не перехватываются: `document.getElementById("notes").focus()`, `computer` `key` = `Left`, снова прочитать `document.querySelector("#deck .fc-meta").textContent` — Expected: `"2 из 9"`.

Урок без карточек: открыть `#m2l1`, подождать 1500 мс:

```js
[!!document.getElementById("s-cards"), Array.prototype.map.call(document.querySelectorAll(".secnav a"), function (a) { return a.textContent; }).indexOf("Карточки")]
```

Expected: `[false, -1]`.

Телефон: `resize_window` `preset: "mobile"`, перезагрузить `#m1l1`, прокрутить к `#s-cards`, открыть ответ первой карточки, `computer` `screenshot`. Expected на снимке: рамка карточки не вылезает за экран, кнопки «Не вспомнил» и «Вспомнил» делят ряд пополам, `.secnav` с пунктом «Карточки» прокручивается по горизонтали. Высота кнопок:

```js
Array.prototype.map.call(document.querySelectorAll("#deck .fc-act .btn"), function (b) { return Math.round(b.getBoundingClientRect().height); })
```

Expected: оба значения ≥ 44.

Тёмная тема: нажать `#themeBtn`, `computer` `screenshot` той же карточки. Expected: карточка на тёмном фоне `--card`, текст читается, «Вспомнил» — светлая ручка с тёмным текстом. Вернуть светлую тему кнопкой `#themeBtn`, `resize_window` `preset: "desktop"`.

`read_console_messages` с `onlyErrors: true` — Expected: ошибок нет.

- [ ] **Step 9: Commit**

```bash
git add app.js styles.css
git commit -m "Колода карточек в уроке

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Карточки уроков 1.2–1.4

**Files:**
- Modify: `content-m1.js` — объекты `m1l2`, `m1l3`, `m1l4`: поле `cards` после `quiz`, первая строка `plan`

**Interfaces:**
- Consumes: формат и проверка из Task 1; колода из Task 3.
- Produces: `window.CONTENT.m1l2.cards`, `.m1l3.cards`, `.m1l4.cards` — по 6–12 `{q, a}`.

Карточки — текст, а не код: готового содержания в плане нет, оно пишется по теории урока по правилам ниже и проверяется скриптом и вычиткой. Образец формата и тона — карточки урока 1.1 из Task 1.

**Правила карточки:**
1. Одна мысль на карточку. Вопрос звучит так, как спросили бы на собеседовании: «Чем…», «Почему…», «Что вернёт…», «Когда…».
2. Ответ — 1–3 предложения или несколько строк кода в `<pre><code>`. Код и имена функций — в `<code>`. Знаки `<` и `>` в тексте — `&lt;` и `&gt;`.
3. Порядок карточек — как разделы `<h3>` в теории. У каждого раздела хотя бы одна карточка; у каждой врезки `callout trap` — отдельная карточка про ловушку.
4. Каждое утверждение в ответе опирается на теорию урока. Фактов, которых нет в теории, не добавлять.
5. Числа — только из теории урока или `ПРОГРЕСС.md` (скрипт проверит).

**Темы по разделам теории:**
- `m1l2` Оконные функции: Чем окно отличается от GROUP BY / Как устроено выражение / Пять функций, которых хватит на 90% задач / Рамка окна: то, что почти всегда работает по умолчанию / CTE: запрос по шагам.
- `m1l3` Агрегации: Гранулярность: главное слово урока / Функции агрегации и их характеры / WHERE и HAVING: когда какой / GROUP BY по нескольким столбцам / Полезные трюки, которые экономят часы.
- `m1l4` CTE: Что такое CTE / Как называть шаги / Когда CTE не нужен / Считаем долю от общего: типовой приём / Рекурсивный CTE: календарь и иерархии.

- [ ] **Step 1: Проверка, которая должна упасть**

Run: `node инструменты/checkcards.js --require m1l2 m1l3 m1l4`
Expected: FAIL, код 1, три строки `нет карточек`.

- [ ] **Step 2: Урок 1.2 — прочитать теорию, написать карточки, поправить план**

Run:
```bash
node -e "global.window={CONTENT:{},SH:new Proxy({},{get:()=>''})};require('./content-m1.js');const C=window.CONTENT.m1l2;console.log(C.theory.replace(/<\/(p|h3|li|tr|pre)>/g,'\n').replace(/<[^>]+>/g,'').replace(/\n\s*\n/g,'\n'))"
```

В `window.CONTENT.m1l2` после `quiz` добавить `cards: [ … ],` — 8–10 карточек, все пять разделов. В `plan` заменить

```js
    { m: "35 мин", w: "Теория: устройство окна и четыре рабочие функции" },
```
на
```js
    { m: "40 мин", w: "Теория и карточки: устройство окна и четыре рабочие функции" },
```

- [ ] **Step 3: Урок 1.3**

Команда из Step 2 с `m1l3` вместо `m1l2`. Добавить `cards` в `window.CONTENT.m1l3`. В `plan` заменить

```js
    { m: "30 мин", w: "Теория: агрегаты, гранулярность, WHERE против HAVING" },
```
на
```js
    { m: "35 мин", w: "Теория и карточки: агрегаты, гранулярность, WHERE против HAVING" },
```

- [ ] **Step 4: Урок 1.4**

Команда из Step 2 с `m1l4`. Добавить `cards` в `window.CONTENT.m1l4`. В `plan` заменить

```js
    { m: "30 мин", w: "Теория: WITH, шаги, именование, рекурсия" },
```
на
```js
    { m: "35 мин", w: "Теория и карточки: WITH, шаги, именование, рекурсия" },
```

- [ ] **Step 5: Проверка проходит**

Run: `node инструменты/checkcards.js --require m1l1 m1l2 m1l3 m1l4`
Expected: PASS, код 0, у каждого из четырёх уроков от 6 до 12 карточек, `Ошибок нет.`

- [ ] **Step 6: Вычитка против теории**

Для каждой карточки уроков 1.2–1.4 найти в выводе теории фразу, на которую она опирается. Карточку без опоры переписать или удалить и повторить Step 5.

- [ ] **Step 7: Проверка в браузере**

Открыть `#m1l2`, `#m1l3`, `#m1l4` (каждый новым адресом, после засева `{ theme: {}, done: {}, review: {} }`). В каждом:

```js
const deck = document.getElementById("deck");
const cards = window.CONTENT[location.hash.slice(1)].cards;
const out = [];
for (let k = 0; k < cards.length; k++) {
  deck.querySelector(".fc-show").click();
  const html = deck.querySelector(".fc-a").innerHTML;
  out.push(html.length > 0 && !/&lt;(code|pre)/.test(html));
  deck.querySelector(".fc-yes").click();
}
[cards.length, out.every(Boolean), deck.querySelector(".fc-done p").textContent]
```

Expected: `[N, true, "Вспомнили N из N. Завтра карточки вернутся в повторение на главной."]` — все ответы открылись, разметка кода не показалась текстом. Если в уроке есть карточка с `<pre>` — `computer` `screenshot` её: код в моноширинном блоке и не вылезает за рамку.

- [ ] **Step 8: Commit**

```bash
git add content-m1.js
git commit -m "Карточки уроков 1.2–1.4

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: Карточки уроков 1.5–1.8

**Files:**
- Modify: `content-m1.js` — объекты `m1l5`, `m1l6`, `m1l7`, `m1l8`: поле `cards` после `quiz`, первая строка `plan`

**Interfaces:**
- Consumes: формат и проверка из Task 1; колода из Task 3.
- Produces: `window.CONTENT.m1l5.cards` … `.m1l8.cards` — по 6–12 `{q, a}`.

Карточки — текст, пишутся по теории урока. Образец формата и тона — карточки урока 1.1 из Task 1.

**Правила карточки:**
1. Одна мысль на карточку. Вопрос звучит так, как спросили бы на собеседовании: «Чем…», «Почему…», «Что вернёт…», «Когда…».
2. Ответ — 1–3 предложения или несколько строк кода в `<pre><code>`. Код и имена функций — в `<code>`. Знаки `<` и `>` в тексте — `&lt;` и `&gt;`.
3. Порядок карточек — как разделы `<h3>` в теории. У каждого раздела хотя бы одна карточка; у каждой врезки `callout trap` — отдельная карточка про ловушку.
4. Каждое утверждение в ответе опирается на теорию урока. Фактов, которых нет в теории, не добавлять.
5. Числа — только из теории урока или `ПРОГРЕСС.md` (скрипт проверит).

**Темы по разделам теории:**
- `m1l5` Подзапросы: Четыре места, где живёт подзапрос / Скалярный подзапрос / IN против EXISTS / Коррелированный подзапрос / Когда подзапрос лишний. Обязательна карточка про `NOT IN` с NULL.
- `m1l6` Даты и когорты: Усечение до периода / Полезные функции SQLite / Когорта: что это такое на самом деле / Время между двумя событиями.
- `m1l7` ClickHouse: Строки против колонок / Что в синтаксисе выглядит иначе / Комбинаторы -If и -Array: главная фишка / Воронки одной функцией / Чего в ClickHouse нет или почти нет.
- `m1l8` Проект, воронка: Что такое воронка / Две конверсии, которые нельзя путать / Порядок работы над задачей. Числа воронки учебной базы — из `ПРОГРЕСС.md` («воронка: visit 220, view_product 206, add_to_cart 175, checkout 121, purchase 64»).

- [ ] **Step 1: Проверка, которая должна упасть**

Run: `node инструменты/checkcards.js --require m1l5 m1l6 m1l7 m1l8`
Expected: FAIL, код 1, четыре строки `нет карточек`.

- [ ] **Step 2: Урок 1.5 — прочитать теорию, написать карточки, поправить план**

Run:
```bash
node -e "global.window={CONTENT:{},SH:new Proxy({},{get:()=>''})};require('./content-m1.js');const C=window.CONTENT.m1l5;console.log(C.theory.replace(/<\/(p|h3|li|tr|pre)>/g,'\n').replace(/<[^>]+>/g,'').replace(/\n\s*\n/g,'\n'))"
```

Добавить `cards` в `window.CONTENT.m1l5`. В `plan` заменить

```js
    { m: "30 мин", w: "Теория: четыре вида подзапросов и NOT IN с NULL" },
```
на
```js
    { m: "35 мин", w: "Теория и карточки: четыре вида подзапросов и NOT IN с NULL" },
```

- [ ] **Step 3: Урок 1.6**

Команда из Step 2 с `m1l6` вместо `m1l5`. Добавить `cards` в `window.CONTENT.m1l6`. В `plan` заменить

```js
    { m: "30 мин", w: "Теория: усечение, разница дат, границы периодов" },
```
на
```js
    { m: "35 мин", w: "Теория и карточки: усечение, разница дат, границы периодов" },
```

- [ ] **Step 4: Урок 1.7**

Команда из Step 2 с `m1l7`. Добавить `cards` в `window.CONTENT.m1l7`. В `plan` заменить

```js
    { m: "40 мин", w: "Теория: колоночное хранение, синтаксис, чего нет" },
```
на
```js
    { m: "45 мин", w: "Теория и карточки: колоночное хранение, синтаксис, чего нет" },
```

- [ ] **Step 5: Урок 1.8**

Команда из Step 2 с `m1l8`. Добавить `cards` в `window.CONTENT.m1l8`. В `plan` заменить

```js
    { m: "20 мин", w: "Теория: как устроена воронка и где в ней врут" },
```
на
```js
    { m: "25 мин", w: "Теория и карточки: как устроена воронка и где в ней врут" },
```

- [ ] **Step 6: Проверка проходит — весь модуль**

Run: `node инструменты/checkcards.js --require m1`
Expected: PASS, код 0, восемь строк `m1lN  K` с K от 6 до 12, `Ошибок нет.`

- [ ] **Step 7: Вычитка против теории**

Для каждой карточки уроков 1.5–1.8 найти в выводе теории фразу, на которую она опирается. Карточку без опоры переписать или удалить и повторить Step 6.

- [ ] **Step 8: Проверка в браузере**

Открыть `#m1l5`, `#m1l6`, `#m1l7`, `#m1l8` (каждый новым адресом, после засева `{ theme: {}, done: {}, review: {} }`). В каждом:

```js
const deck = document.getElementById("deck");
const cards = window.CONTENT[location.hash.slice(1)].cards;
const out = [];
for (let k = 0; k < cards.length; k++) {
  deck.querySelector(".fc-show").click();
  const html = deck.querySelector(".fc-a").innerHTML;
  out.push(html.length > 0 && !/&lt;(code|pre)/.test(html));
  deck.querySelector(".fc-yes").click();
}
[cards.length, out.every(Boolean), deck.querySelector(".fc-done p").textContent]
```

Expected: `[N, true, "Вспомнили N из N. Завтра карточки вернутся в повторение на главной."]`. Урок 1.7 текстовый (`kind: "text"`) — секция карточек стоит там же, между теорией и задачей.

- [ ] **Step 9: Commit**

```bash
git add content-m1.js
git commit -m "Карточки уроков 1.5–1.8

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Выпуск — версия, документация, сборка, PR

**Files:**
- Modify: `sw.js:26` — `VERSION`
- Modify: `README.md` — раздел «Прогресс» (после абзаца про повторение) и раздел «Для себя: как править курс»
- Modify: `ПРОГРЕСС.md` — «Структура урока в content-*.js» и новый раздел «Карточки»
- Modify: `Тетрадь аналитика.html` — пересборка
- Add: `docs/superpowers/plans/2026-09-17-cards.md` (этот план)

**Interfaces:**
- Consumes: всё из Task 1–5.
- Produces: собранный файл, PR `cards-pilot` → `design-search-offline`.

- [ ] **Step 1: Версия кеша**

В `sw.js` заменить `const VERSION = "2026-09-16";` на `const VERSION = "2026-09-17";`.

- [ ] **Step 2: README — для ученика**

В `README.md` после абзаца, который начинается с «Вопросы из самопроверки возвращаются на главную через 1, 3, 7 и 21 день.», добавить абзац:

```markdown
В уроках модуля 1 после теории стоят карточки «вопрос → ответ»: сначала
ученик вспоминает ответ сам, потом открывает его и отмечает «вспомнил»
или «не вспомнил». Карточки возвращаются в то же повторение на главной,
вперемешку с вопросами самопроверки и по той же лестнице. Сколько раз
человек не вспомнил карточку или ошибся в вопросе, тоже сохраняется.
```

- [ ] **Step 3: README — для автора**

В таблице раздела «Для себя: как править курс» заменить строку

```markdown
| `content-m1.js` … `content-m6.js` | содержание уроков по модулям: теория, задача, эталон, подсказки, решение |
```
на
```markdown
| `content-m1.js` … `content-m6.js` | содержание уроков по модулям: теория, задача, эталон, подсказки, решение, карточки |
```

После подраздела «### Как открыть новый урок» (перед `---`) добавить подраздел:

```markdown
### Как добавить карточки

Карточки — поле `cards` в объекте урока, рядом с `quiz`:

    cards: [
      { q: "Чем LEFT JOIN отличается от INNER JOIN?",
        a: "INNER оставляет только строки с парой с обеих сторон…" }
    ]

От 6 до 12 на урок, одна мысль на карточку, порядок — как в теории.
Новые карточки дописывайте в конец массива: номер карточки — её ключ
в расписании повторения. Правила написания — в `ПРОГРЕСС.md`, раздел
«Карточки». Перед сборкой:

    node инструменты/checkcards.js --require m1

Скрипт проверяет формат, длину, разрешённые теги (`<code>`, `<pre>`),
посторонние символы и то, что каждое число из ответа есть в теории урока
или в `ПРОГРЕСС.md`.
```

- [ ] **Step 4: ПРОГРЕСС.md**

В разделе «## Структура урока в content-*.js» заменить строку

```
    quiz[{q,opts,right,why}],
```
на
```
    quiz[{q,opts,right,why}],
    cards[{q,a}],
```

Перед разделом «## Правила, которые надо соблюдать» добавить:

```markdown
## Карточки

Состояние: модуль 1 — готово (пилот), модули 2–6 — ждут одобрения пилота.
Спецификация: `docs/superpowers/specs/2026-09-17-cards-design.md`.

Правила написания:
- 6–12 карточек на урок, одна мысль на карточку.
- Вопрос — как на собеседовании: «Чем…», «Почему…», «Что вернёт…».
- Ответ — 1–3 предложения или несколько строк кода; `q` до 160 знаков,
  `a` до 400. Разметка — только `<code>` и `<pre><code>`.
- Порядок — как разделы `<h3>` теории; у каждого раздела хотя бы одна
  карточка, у каждой врезки `callout trap` — своя.
- Факты — только из теории урока. Числа — только из теории урока или
  из этого файла.
- Проверка: `node инструменты/checkcards.js --require mN`.

Хранение: корзина `review`, ключ `урок:cномер`. Номер — позиция в массиве,
поэтому новые карточки дописывают в конец: вставка в середину сдвинет
расписание уже начатых. Удалённая карточка из очереди пропадает сама.
`miss` в записи — сколько раз не вспомнил или ошибся (и для вопросов
самопроверки) — задел для страницы «Мои ошибки».
```

- [ ] **Step 5: Итоговая проверка содержания**

Run: `node инструменты/checkcards.js --require m1`
Expected: PASS, код 0, `Ошибок нет.`

- [ ] **Step 6: Перенос прогресса файлом**

На `course-dev` засеять `{ theme: {}, done: {}, review: {} }`, затем на главной:

```js
const wait = function (ms) { return new Promise(function (r) { setTimeout(r, ms); }); };
const f = new File([JSON.stringify({ done: { m1l1: 1726000000000 }, review: { "m1l1:c2": { step: 1, due: "2026-09-20", miss: 1 } } })],
  "p.json", { type: "application/json" });
const dt = new DataTransfer(); dt.items.add(f);
window.dispatchEvent(new DragEvent("drop", { dataTransfer: dt, cancelable: true }));
await wait(600);
JSON.parse(localStorage.getItem("da.state.v1")).review["m1l1:c2"]
```

Expected: `{ step: 1, due: "2026-09-20", miss: 1 }`.

- [ ] **Step 7: Сборка и проверка файла с диска**

Run: `python3 build.py`
Expected: без ошибок, файл `Тетрадь аналитика.html` обновлён.

`navigate` на `file:///Users/nekozlovskiy/Desktop/Сайт для подготовки к jun analyst/Тетрадь аналитика.html#m1l2`, подождать 1500 мс:

```js
[!!document.getElementById("s-cards"), document.querySelector("#deck .fc-meta").textContent, window.CONTENT.m1l2.cards.length]
```

Expected: `[true, "1 из N", N]`. `read_console_messages` с `onlyErrors: true` — ошибок, связанных с карточками, нет.

- [ ] **Step 8: Commit**

```bash
git add sw.js README.md ПРОГРЕСС.md "Тетрадь аналитика.html" docs/superpowers/plans/2026-09-17-cards.md
git commit -m "Карточки: версия кеша, документация, сборка

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 9: PR**

```bash
git push -u origin cards-pilot
```

```bash
gh pr create --base design-search-offline --head cards-pilot \
  --title "Карточки «вопрос → ответ»: пилот на модуле 1" \
  --body "$(cat <<'EOF'
## Что сделано

- В уроках модуля 1 после теории — колода карточек «вопрос → ответ»: вспомнить, открыть ответ, оценить себя.
- Карточки возвращаются в общее повторение на главной вперемешку с вопросами самопроверки, по лестнице 1/3/7/21.
- В запись повторения добавлен счётчик ошибок `miss` — задел для страницы «Мои ошибки».
- Скрипт `инструменты/checkcards.js` проверяет формат карточек и то, что числа в ответах взяты из проверенных.

База PR — `design-search-offline` (PR #1): карточки опираются на его оформление. После слияния PR #1 база переключится на `main`.

## Проверка

- `node инструменты/checkcards.js --require m1` — ошибок нет.
- В браузере: колода в уроке, «ещё раз», повторное открытие урока, смешанная очередь на главной, лимит 20, клавиши, 375px, тёмная тема, перенос прогресса файлом, собранный файл с диска.

Спецификация: `docs/superpowers/specs/2026-09-17-cards-design.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: ссылка на новый PR.
